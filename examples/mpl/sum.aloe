(define-class Sum Math
  (fields
    (terms (List Math))
    (const Int))
  (methods
    (unwrap () Math
      (if ((self const) = 0)
          (if (((self terms) len) = 1)
              ((self terms) first)
              self)
          self))))
