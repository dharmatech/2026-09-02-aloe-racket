(define-class Pow Math
  (fields
    (base Math)
    (exp Int))
  (methods
    (unwrap () Math
      (if ((self exp) = 0)
          (Num new 1)
          (if ((self exp) = 1)
              (self base)
              self)))))
