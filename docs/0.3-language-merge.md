# Charter — 0.3 language merge

**Status.** Assignment for one merge conversation. Not Aloe law.
Not a language-feature checkpoint. Not a redesign.

**Your job.** Land the already-implemented constructor / `case` /
Option / filesystem work onto local `main`. Then **stop**.

If you have been told to read this file, this is the whole assignment.

This is **one conversation**. It is not a designer spec, not a
checkpoint-manager split, and not a series of implementer chats.
The language work is already done on `experiment/filesystem`
(checkpoints 89–107). You are moving `main` and writing landing
docs. If `--ff-only` or tests fail, stop and report to the human;
do not invent slices.

Do not write a new language spec. Do not invent checkpoints 108+ on
`main`. Do not merge Gel-directory, Gel-presentations, editor, or LSP.
Do not strip Mirror, Signature, or Gel. Do not implement Proposal A.
Do not start MPL-ADT. Do not slim or rewrite `aloe/type.rkt`,
`aloe/eval.rkt`, or `aloe/parse.rkt`.

The human will review the result in the parent high-level conversation.

---

## 1. How this conversation works

1. Read this charter. Confirm the facts in §3 against the live repo.
   If a SHA or ancestor check fails, **stop and report**. Do not improvise
   a rebase.
2. Run the preflight in §6.
3. Fast-forward local `main` to `experiment/filesystem` (§7).
4. Make the landing docs commit on `main` (§8). Include this file
   on `main` if it is not already there.
5. Run tests (§9).
6. Write the completion report (§10). Stop.

Do not push. Do not force-push. Do not publish a GitHub PR unless the
human explicitly asks in this conversation.

---

## 2. Why this merge exists

Aloe grew a deep experiment stack off `main`. Two questions got
entangled:

1. Keep Proposal B constructors (`case`, Option, the filesystem
   libraries that exercise them)?
2. Is the Racket kernel still small enough, given editor/LSP growth
   and Mirror/Gel reflection?

The parent discussion answered (1) **yes, land it**, and deferred (2).
This assignment does only (1).

Spoken name: **0.3 language merge**. That is the bundle name. It is
not a SPEC retitle. On `experiment/filesystem`, `SPEC.md` already
calls constructors **0.4** because Mirror/Signature were labeled 0.3
and host capabilities 0.4-adjacent as §14. **Do not renumber SPEC
sections in this assignment.**

---

## 3. Facts to verify before touching git

Record the SHAs you actually see. The numbers below were true when
this charter was written (2026-09-19). If they drifted, stop.

| Ref | Expected SHA (prefix) | Role |
|---|---|---|
| local `main` | `0d01895` | Destination. Host boundary sealed. Mirror/Gel already here. **No constructors.** |
| `experiment/filesystem` | `8b0a110` | **Source tip.** Constructors through checkpoint 107. |
| `origin/main` | `c2baa20` | **Not the destination.** Diverged. See §11. |

Must be true:

```sh
git merge-base --is-ancestor main experiment/filesystem
```

That check means the merge is a **fast-forward**. Local `main` is
already an ancestor of `experiment/filesystem`. Checkpoint 97 already
merged this `main` (host boundary) into the constructor line.

Kernel delta of this merge (`git diff --stat main...experiment/filesystem -- aloe`):

- `aloe/parse.rkt`, `aloe/eval.rkt`, `aloe/type.rkt`, and a small
  `aloe/host.rkt` change
- about **+481 / −77** in those files
- no `aloe/lsp.rkt`, no second parser, no editor query files

If `aloe/lsp.rkt` or `aloe/parse.rkt` growing past the filesystem tip
appears in the merge diff, you grabbed the wrong branch.

---

## 4. In scope (land this)

From `experiment/filesystem`, which already contains:

**Language (checkpoints 89–96)**

- Proposal B class constructors: `(fields …)` remains singleton `new`;
  `(constructors …)` declares named payloads
- Receiver-anchored `case`
- Constructor identity on instances
- Option and Tree goldens
- Ratified `SPEC.md` text for the above
- `docs/class-constructors.md` kept as historical design notes
- Decision: Proposal B. Proposal A (`codex/unified-nominal-adts`) is
  **not authority**

**Host integration already done on that line (97–99)**

- Merge of sealed typed host boundary from `main`
- `(List String)` crossing
- Generic host-type retention

**Libraries and filesystem (100–107)**

- `lib/option.aloe` — loadable, not a default-driver binding
- `host/racket/fs.rkt` and `host/racket/fs-repl.rkt`
- `lib/fs.aloe` — `Path`, `Entry`, `Fs`
- `lib/disk.aloe` — `Disk`, `Location`, live `Item` classes,
  `Directory.entries`
- Tests `tests/checkpoint-90.rkt` through `tests/checkpoint-107.rkt`
  and `tests/fs-repl.rkt`
- Designer/vocabulary docs that those checkpoints already added

Default drivers still receive **no** `fs-host` and **no** Term.
Do not change that.

---

## 5. Out of scope (do not land, do not start)

These live **later** on the stack. They are **not** ancestors of
`experiment/filesystem`; they are descendants. Fast-forwarding `main`
to filesystem leaves them on their branches.

| Leave on its branch | Tip / notes |
|---|---|
| `experiment/gel-directory-surface` | Checkpoints 108–117, String `len`/`take`, `gel-directory-run` |
| `experiment/2026-09-11-gel-presentations` | Local gel-presentations series |
| `experiment/2026-09-12-editor` | Source locations, signature catalog, expression query, LSP, VS Code |
| `codex/unified-nominal-adts` | Proposal A. Rejected for this line. |
| `fix/boid-monomorphic` | Unrelated |

Also out of scope **even if tempting**:

- Stripping `Mirror` / `Signature` / Gel from `main`. They are
  **already on `main`**. This merge does not add them. Removing them
  is a different project: it is not a fast-forward, it would replay
  constructors and filesystem onto a pre-mirror history, and it would
  strand the later experiment stack. The parent discussion deferred
  that question. Do not decide it here.
- Unifying the two parsers in `parse.rkt`. That duplication does not
  exist at `experiment/filesystem`; it starts at editor source-locations.
- Observer / `type-signature-specs` extraction from `type.rkt`. Same:
  editor branch only.
- MPL-ADT, Allo Emacs, new applications.
- Rewriting the kernel for elegance.
- Creating global checkpoint 118.
- Merging `origin/main` or pushing.

After this assignment, `main` will still contain Mirror, Signature,
and the earlier Gel (stack / menus / `gel-run`) that `main` already
had. That is correct for this merge.

---

## 6. Preflight

Working directory: `/home/dharmatech/journal/2026-09-02-aloe-racket`.

1. `git status`. If there is **committed** work you would lose, stop.
   Untracked editor scratch (for example
   `examples/test-intellisense-001.aloe`) is **not** part of this merge.
   Do not add it. Do not delete it unless the human says so.
2. Confirm you are not about to check out `main` on top of unrelated
   staged edits. If the index is dirty with editor or Gel files, stop
   and report.
3. Confirm the ancestor check in §3.
4. `git log --oneline main..experiment/filesystem` should start at the
   constructor docs / checkpoints 89–107 / filesystem commits, including
   `0c3c5ed` (checkpoint 97 merge of `main`). It must **not** include
   Gel-directory, presentations, or editor commits.
5. Do not `git checkout experiment/2026-09-12-editor` as the source.

---

## 7. Merge procedure

```sh
git checkout main
git merge --ff-only experiment/filesystem
```

`--ff-only` is required. If it refuses, **stop and report**. Do not
fall back to a merge commit, rebase, or cherry-pick.

After a successful fast-forward:

- `git rev-parse main` equals `git rev-parse experiment/filesystem`
- `git merge-base --is-ancestor experiment/class-constructors main`
- `git merge-base --is-ancestor experiment/host-boundary main`

Those experiment branches become ancestors of `main`. Leave the branch
names in place. Do not delete them in this assignment.

Do **not** merge `experiment/gel-directory-surface` or
`experiment/2026-09-12-editor` into `main`.

---

## 8. Landing docs (one commit on `main`)

The fast-forward brings `docs/handoff.md` that still says constructors
have **not** been merged to `main`, and a README that still describes
Aloe as 0.2-only. Fix that in **one** commit. Do not rewrite history
of checkpoints 89–107.

### 8.1 This charter

Copy `docs/0.3-language-merge.md` onto `main` if the fast-forward did
not already contain it (it was authored on the editor-stack working
tree). Keep the body. Set the status line at the top to:

> **Status.** Landed on `main` at \<sha\>. Historical merge assignment.

### 8.2 `docs/handoff.md`

Edit the filesystem-branch handoff that just arrived:

- Remove or rewrite the `## \`experiment/filesystem\`` section. That
  work is now `main`. Do not leave “Constructors have not been merged
  to `main`.”
- Retitle **Current state** so it is current state of `main` through
  checkpoint 107, not “0.4 on `experiment/filesystem`.”
- Keep the constructor, Option, `lib/fs.aloe`, `lib/disk.aloe`, and
  typed-host bullets that are already accurate.
- Do **not** paste Gel-directory, presentations, or editor/LSP current
  state from later branches. Those are not on `main` after this merge.
- It is correct to mention that Mirror, Signature, and the earlier Gel
  remain on `main` (they already were).

### 8.3 `README.md`

Small updates only:

- Status: still an exploratory prototype. Do not announce a finished
  public 0.3 release.
- Layout: `SPEC.md` is no longer “0.1 plus 0.2 additions” only. Point
  at constructors / `case`, `lib/option.aloe`, and the filesystem
  libraries.
- Do not claim editor/LSP or Gel-directory.

### 8.4 Journal (optional but preferred)

Add a short `docs/journal/` note that this landing happened: Proposal B
constructors, Option library, filesystem host and Aloe libraries, tests
through 107. Driving applications: Option goldens, Tree golden, the two
filesystem APIs. Not in this landing: Gel-directory, editor, kernel
rewrite.

Follow the tone of `docs/journal/2026-09-04-v0.2.md`. Do not dump
line-count tables unless you measure them on **this** `main` after the
fast-forward.

### 8.5 Do not edit

- `SPEC.md` (already ratified on the source branch)
- `aloe/**`
- `lib/**`
- `host/**`
- `tests/**` except if a test path breaks solely because of the docs
  commit (it should not)
- `docs/class-constructors.md` except you may add one line that the
  design is now law on `main` via SPEC, still historical notes
- `docs/editor/**`, `docs/gel/**`

Commit message, one commit:

```text
Record constructors and filesystem as landed on main
```

---

## 9. Tests

From the project root, on `main` after the docs commit:

```sh
raco test tests
```

Also run the filesystem REPL test file if `raco test tests` does not
already pick it up:

```sh
raco test tests/fs-repl.rkt
```

Green is required. If red, **stop and report**. Do not “fix” by
changing constructor or filesystem semantics. Do not skip tests.

Spot-check, once, by hand or `eval-source`:

- `(Point new 1 2)` still works (one-constructor classes)
- loading `lib/option.aloe` and constructing `(Option Some 1)` /
  `(Option None)`
- default `./bin/aloe` still has no `fs-host`

Do not start Boids, Gel, or VS Code as part of this assignment.

---

## 10. Completion report

End the conversation with a short report the human can paste back
into the parent discussion:

- `main` SHA before and after
- whether `--ff-only` succeeded
- docs commit SHA
- `raco test tests` result
- confirmation that `aloe/lsp.rkt` is **absent** on `main`
- confirmation that `lib/option.aloe`, `lib/fs.aloe`, `lib/disk.aloe`
  are **present**
- reminder that `origin/main` was not updated (§11)
- leftover branches still **not** on `main`: gel-directory,
  gel-presentations, editor

Then stop.

---

## 11. `origin/main` (do not fix here)

Local `main` and `origin/main` have **diverged**:

- local `main` (before this merge) has typed-host checkpoints 83–88
  and the handoff update, which `origin/main` lacks
- `origin/main` has a constructor-docs commit and its revert
  (`442cb93`, `c2baa20`), which local `main` lacks, but
  `experiment/filesystem` already contains those two commits

Pushing after the fast-forward is a **human** git decision (likely
needs a non-fast-forward update of `origin/main`). This assignment
does not push. If the human later asks to push, use the local GitHub
CLI skill and do not force-push unless they say the exact words.

---

## 12. What this merge does *not* decide

Leave these for the parent conversation. Do not open them.

1. **Keep Mirror / Gel on `main` long term?** Already present.
   Removing them is history surgery, not a follow-on fast-forward.
   Allo Emacs may want some reflection later; that is not a reason to
   strip or to grow Gel-directory into this landing.
2. **Editor / LSP?** Wants a later elegance pass (one parser; checker
   observers not living in the middle of `type.rkt`). Can stay on
   `experiment/2026-09-12-editor`, which remains stacked on the
   filesystem tip. After this merge that tip **is** `main` plus the
   landing docs commit, so the editor branch is at most one docs
   commit behind. Do not merge it. Do not merge `main` back into it
   unless the human asks.
3. **Kernel slimming on `main` itself?** `eval.rkt` already grew on
   `main` for Mirror/Signature. Separate discussion.
4. **MPL-ADT / more ADT applications?** Good exercise, not a merge
   gate.

---

## 13. Authority

Read, in order:

- this file
- `SPEC.md` on `experiment/filesystem` (law for the code you land)
- `docs/philosophy.md`
- `docs/decisions.md` — “Class constructors (2026-09-07)” and typed
  host decisions
- `AGENTS.md`
- `docs/class-constructors.md` as history, not a second spec
- checkpoints 89–107 as already-complete history

Not authority:

- `codex/unified-nominal-adts`
- `docs/editor/**`
- Gel-directory / presentations specs
- the parent chat transcript, except as this charter records it
