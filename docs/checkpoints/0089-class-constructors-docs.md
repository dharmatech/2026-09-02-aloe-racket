# Checkpoint 89 — Proposal B documents

**Branch.** `experiment/class-constructors`

**Depends on.** Checkpoint 88 / current `main`

**Status.** Complete (documents only)

## Goal

Add the Proposal B design notes. No language, checker, evaluator, example,
or test change.

## Authority

- Law for running code: `SPEC.md` on this branch (Aloe through checkpoint 88).
- Design to implement next: `docs/class-constructors.md`.
- Not authority: `codex/unified-nominal-adts` (Proposal A).

## Files

Create:

- `docs/class-constructors.md`
- this checkpoint file

Do not edit `SPEC.md`, `aloe/`, `examples/`, `lib/`, `gel/`, or `tests/`
in this checkpoint.

## Done when

The two documents are on the branch. `raco test` is still green.
No new syntax is accepted.
